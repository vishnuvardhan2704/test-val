# Groq API key (the LLM client in app/agents/gemini_client.py talks to Groq's
# OpenAI-compatible endpoint, despite the module's filename).
GROQ_API_KEY=
# Optional, defaults to a Groq-hosted Llama model if unset — see app/config.py
GROQ_MODEL=
